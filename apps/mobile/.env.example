# See docs/google-drive-mobile-setup.md for how to obtain this. Different Client ID *type* than
# the web app's — an Android OAuth client, tied to a signing key's SHA-1 fingerprint.
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=
